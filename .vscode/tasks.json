{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    { "type": "npm", "script": "build", "group": "build" },
    { "type": "npm", "script": "clean", "group": "build" },
    { "type": "npm", "script": "watch", "group": "build" },
    {
      "type": "shell",
      "command": "npx lerna publish --registry https://registry.npmjs.org",
      "label": "项目发布",
      "group": "build"
    }
  ]
}
